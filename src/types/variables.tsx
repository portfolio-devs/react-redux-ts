type username = Record<string, any>
type loggedType = Record<string, string>
type componentsType = Record<string, any>
type Inputs = {
  example: string,
  exampleRequired: string,
};

export type { username, componentsType, loggedType, Inputs }